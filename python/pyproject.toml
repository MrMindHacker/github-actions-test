[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "flake8>=7.3.0",
    "pip-audit>=2.10.1",
    "pytest>=9.1.1",
]
